# M9 diagnostics: message, cause, fix

*GENERATED by `tools/mkdiagnose.py` from `probes/` -- do not edit.*

Every entry is one checker refusal.  The **message** is read from the
probe's first line, and `probediff.sh` holds both checkers to it, so
this column cannot drift from what `m9c` prints.  The cause and the
fix are curated in the generator, keyed by probe name; a probe without
an entry fails the build.  `runtime/test/diagdiff.sh` is the gate.

A checker message is printed as `LINE:COL Module.Proc: MESSAGE`, then
`m9c: N errors in FILE`.  A PARSE error is still a bare count with no
line (`m9c: 4 parse errors in FILE`): run `host/fpc/g1 FILE` for
`LINE:COL message`.  A GENERATOR error is `FILE:LINE: gen: MESSAGE`.

| message | cause | fix | probe |
|---|---|---|---|
| `ADR exists only inside UNSAFE modules` | ADR takes the address of a value, which is the one thing the memory rules cannot check | put the procedure in an UNSAFE module, or pass a SLICE/VAR instead of an address (par 7) | `adr-outside-unsafe` |
| `unknown procedure: Pair` | a type alias converts under its own name only when it aliases a scalar; a record alias is not a conversion | construct the record; conversions are for numeric widths and CHAR | `alias-nonscalar-conversion` |
| `cannot assign ALL to I64` | ALL is the axis-keeping marker for VIEW and has no value outside one | use ALL only as a VIEW argument; a whole-axis loop is FOR i := 0 TO LEN (g, k) - 1 | `all-outside-a-view` |
| `G expects 1 argument(s), got 2` | the call passes a different number of arguments from the declaration | read the signature in docs/modules/<M>.md; every parameter is positional and required | `arity-mismatch` |
| `BYTE is a raw octet: no arithmetic` | BYTE is a raw octet, not a number: no +, -, comparison as magnitude | convert with U8 (b) or I64 (b) first, and back with BYTE (x), each of which RAISES ValueRange | `byte-arithmetic` |
| `CASE label is a CHAR/string literal but the selector is I64` | the CASE label's type is not the selector's type | labels must be literals or CONSTs of the selector's type; a CHAR selector takes 'x' or 41C | `case-label-mismatch` |
| `CASE label 200 appears twice` | the same scalar label appears in two arms; the second is dead and one of the two is a typo -- decided at compile time | remove the duplicate, or widen it to a range if that was meant | `case-label-twice` |
| `CASE RECORD is reached by CASE, not by selection` | the variant payload of a CASE RECORD is reached only through a CASE arm that binds it | write CASE v OF | Kind.Str (s) : ... END; never v.field on the variant part | `case-record-selected` |
| `cannot compare I64 with SLICE OF CHAR` | the two sides of a comparison have different types and nothing converts implicitly | convert one side explicitly; for strings use DynStr.Eq / Text.Eq, not = (par 2.1) | `compare-mismatch` |
| `cannot concatenate a string with CHAR` | + concatenates STRINGS; a CHAR variable is not a string (a 1-char literal is) | append the CHAR with DynStr.AppendChar, or make it a one-character string first | `concat-char` |
| `cannot concatenate a string with an integer literal` | + on strings takes strings on both sides; an integer literal is not one | format first: Fmt.I64Str (n) or DynStr.AppendI64 | `concat-non-string` |
| `condition must be BOOL, not I64` | IF/WHILE/ELSIF take a BOOL; an integer is not implicitly a truth value | write the comparison out: IF n # 0 THEN | `cond-not-bool` |
| `use of s after it was consumed by SHARED` | SHARED (s) consumed s on one arm, so after the join s may be gone (moved in ANY arm = moved after) | share on every path, or share before the branch; see par 4.2 | `conditional-move` |
| `declared in the definition but not implemented` | the DEFINITION declares a procedure the IMPLEMENTATION never defines | implement it, or remove it from the definition; the signature is the contract (par 3) | `def-not-implemented` |
| `a borrow is not yours to free` | the value came in as a value/VAR/RO parameter -- a borrow -- and a borrow is not yours to free | only OWN parameters and locals holding owned PTRs may be DISPOSEd; move ownership with OWN | `dispose-a-borrow` |
| `the pool owns p; free the pool` | PTR T IN pool is carved from a pool and the pool frees it as a whole | never DISPOSE a pool-interior pointer; free the pool (par 4.3, docs/pools.md) | `dispose-pool-interior` |
| `DIV is integer division` | DIV and MOD are integer operators | use / for floats; Math.Fmod for a float remainder | `div-on-float` |
| `axis 2 of a GRID 2 OF F64 does not exist` | LEN (g, k) names an axis the grid's rank does not have (axes are 0-based) | a GRID 2 has axes 0 and 1 | `grid-len-axis-exists` |
| `LEN of a GRID needs an axis` | a GRID has one extent per axis, so LEN needs to be told which | LEN (g, 0); LEN (s) without an axis is for slices | `grid-len-needs-an-axis` |
| `cannot assign GRID 3 OF F64 to GRID 2 OF F64` | GRID 3 OF F64 and GRID 2 OF F64 are different types; rank is in the type, shape in the value | declare the matching rank, or take a VIEW that drops an axis | `grid-rank-is-part-of-the-type` |
| `a GRID 3 needs 3 subscripts, not 2` | every axis must be subscripted; a GRID is not a nested array | g[i, j, k] for a GRID 3; VIEW (g, i, ALL, ALL) to select a plane | `grid-subscript-arity` |
| `cannot assign I32 to I64` | there is no implicit conversion between any two numeric types, widening included | I64 (x) explicitly; the conversion RAISES ValueRange, which your RAISES set must carry (par 2.1) | `implicit-widening` |
| `cannot assign an integer literal to F64` | an integer literal fits any integer type but not a float | write 0.0, 1.0E-6; a <real> literal adapts to F32 or F64 | `int-literal-to-float` |
| `float division; integers use DIV` | / is float division | DIV for integers | `int-slash-division` |
| `IS SOME needs an OPT operand` | IS SOME is the guard for OPT; the operand is not OPT (a cross-module PTR T IN pool may type as unknown and NOT be diagnosed -- check the declaration) | declare the type OPT PTR T, or drop the guard if it cannot be absent | `is-some-on-non-opt` |
| `argument 1 of Note: borrowed msg is kept by the callee -- declare KEPT msg (par 4.1)` | a borrowed parameter is passed to a parameter the callee declares KEPT, so the caller is retaining it too | declare the caller's own parameter KEPT as well -- the declaration composes upward, exactly as RAISES does (par 4.1, docs/retention.md) | `kept-borrow-arg` |
| `argument 1 of Note: a KEPT parameter cannot take a concatenation -- it dies with this frame (par 4.1)` | a + result lives in the frame's arena and dies at RETURN, but the callee declares it will keep the argument | build the string in a pool that outlives the retention (DynStr into a caller-supplied pool, or HEAP) and pass that (par 2.3, par 4.1) | `kept-concat-arg` |
| `undeclared retention: borrowed msg reaches module state -- declare KEPT msg (par 4.1)` | a borrowed parameter is stored somewhere that outlives the call -- module state, the caller's storage, or the answer -- and the signature does not say so | declare the parameter KEPT so every caller can see the retention, or copy the bytes instead of keeping the borrow (par 4.1, docs/retention.md) | `kept-undeclared` |
| `undeclared retention: borrowed msg (carried by t) reaches module state -- declare KEPT msg (par 4.1)` | a borrowed parameter was copied into a local (or bound by IS SOME or a CASE pattern) and the copy was stored somewhere that outlives the call -- the local carried the borrow | the retention is real even though indirect: declare the parameter KEPT, or copy the bytes instead of the reference (par 4.1, docs/retention.md) | `kept-via-local` |
| `cannot lend the value parameter p as VAR` | a value PTR parameter is a shared borrow; passing it on as VAR would launder it into a mutable one | take the parameter as VAR yourself if you need to pass it as VAR (par 4.1) | `lend-value-ptr-as-var` |
| `a local CONST may not shadow a module CONST: Tag` | a procedure declares a CONST with the same name as one the module already declares | rename one of them.  Which would win depends on lookup order, and the map answers the first hit, so the shadow is refused rather than resolved (docs/frame-pools.md) | `local-const-shadow` |
| `module-level state requires STATEFUL on the definition` | a module-level VAR is state, and a module with state must say so | add [STATEFUL] to the DEFINITION MODULE, or move the state into a record the caller owns (par 6) | `module-state-without-stateful` |
| `monitor field n is reached from outside a procedure bound to the monitor (par 6)` | a monitor serialises access by letting only its BOUND procedures reach its fields, and the binding is the FIRST parameter (par 6) | add a short bound procedure -- PROCEDURE Count (VAR g: Gate) : I64 -- and call that instead | `monitor-outside` |
| `cannot move borrowed q into an OWN parameter` | an OWN parameter takes ownership, and a borrow has none to give | pass something you own -- a local, an OWN parameter -- or take the argument as VAR instead | `move-borrow-into-own` |
| `NEW takes the pool first, then the type` | NEW's arguments are the wrong way round: the pool comes first, then the type | NEW (pool, T) for a pointer, NEW (pool, T, n) for a slice, NEW (pool, T, n1, n2) for a grid (par 4.3) | `new-reversed` |
| `no field w` | the record has no field of that name | read the record in docs/modules/<M>.md; a variant's payload is reached through CASE | `no-such-field` |
| `opaque type not defined in the implementation` | the DEFINITION declares an opaque TYPE the IMPLEMENTATION never completes | TYPE T = RECORD ... END in the implementation | `opaque-not-defined` |
| `OPT value used without IS SOME guard` | an OPT field was read without IS SOME; OPT is traced through fields, not only names | IF r.f IS SOME p THEN ... END, and use p inside (par 2.2) | `opt-through-field` |
| `pool-interior pointer escapes its pool` | PTR T IN pool cannot outlive its pool, and this pool dies with the frame | take the pool as a VAR parameter so the caller owns it, or return by value (docs/pools.md) | `pool-escape-on-return` |
| `cannot allocate from the pool pool in a PURE procedure (par 3.2)` | NEW from a pool the CALLER owns consumes the caller's storage and answers a slice into the caller's arena -- an effect (par 3.2) | use a local VAR scratch: POOL, or drop [PURE] | `pure-allocates` |
| `PURE procedure calls Note, which is not PURE (par 3.2)` | a PURE procedure may call only PURE procedures -- which is what makes 'no I/O' true without the checker knowing what I/O is, since a foreign procedure is [SERIAL] or [REENTRANT] and never [PURE] | declare the callee [PURE] too if it really is, or drop [PURE] from the caller | `pure-calls-impure` |
| `cannot write through the VAR parameter acc in a PURE procedure (par 3.2)` | a PURE procedure has no observable effect (par 3.2), and writing through a caller's VAR binding is precisely what the caller observes | answer a value instead of writing through a parameter, or drop [PURE] | `pure-writes-var` |
| `function result discarded: G returns I64` | a function's result was not used; M9 does not let a value fall on the floor | assign it, or make the procedure proper if the result is not wanted | `result-discarded` |
| `retention: borrowed x stored into r` *(ledger, not an error)* | NOT an error: the P3 ledger noting that a borrowed parameter was stored beyond the frame | nothing to fix; it is measured against the kill-gate (docs/p3-ledger.md) | `retention-ledgered` |
| `cannot RETURN SLICE OF CHAR from a function of type I64` | the RETURN's type is not the function's declared result type | convert explicitly, or change the declaration | `return-mismatch` |
| `RETURN with a value in a proper procedure` | a proper procedure (no ': T') cannot RETURN a value | declare a result type, or RETURN without a value | `return-value-in-proper-proc` |
| `signature differs from definition` | the IMPLEMENTATION's procedure heading is not the DEFINITION's, printed canonically (modes, types, RAISES all count) | copy the definition's heading exactly; the IMPLEMENTATION adds only '=' | `signature-differs-from-definition` |
| `is not total (missing R)` | CASE over a CASE RECORD must name every variant of the selector's own type | add the missing arm; there is no ELSE for variants (par 8) | `totality-uses-selector-type` |
| `unknown procedure: Nonexistent` | no procedure of that name is visible -- MOST OFTEN a real name whose module is not IMPORTed in THIS module (an IMPLEMENTATION has its own IMPORT list), otherwise a guessed name | add IMPORT M to the implementation; then confirm the name in docs/modules/M.md or with m9c --doc | `unknown-callee` |
| `use of s after it was DISPOSEd` | the name was DISPOSEd on an earlier line and is dead | do not read it; re-assign to bring it back to life (par 4.2) | `use-after-dispose` |
| `use of s after it was moved` | assigning a bare owned pointer to another name MOVES it; the source is dead | use the destination; if both must live, that is a SHARED handle | `use-after-move-assign` |
| `use of s after it was consumed by SHARED` | SHARED (s) consumed s; the handle is what lives on | use the SHARED result; s is gone | `use-after-shared` |
| `must be a variable (VAR/OWN parameter)` | a VAR or OWN argument must be something that can be written to -- a variable, field or element | store the expression in a local first | `var-arg-not-designator` |
| `needs 3 axis arguments, not 2` | VIEW takes exactly one argument per axis of the grid: an index (drops it) or ALL (keeps it) | VIEW (g, i, ALL, ALL) for a GRID 3 | `view-axis-count` |
| `a VIEW that keeps no axis is an index` | a VIEW with an index on every axis is a single element, not a view | subscript instead: g[i, j] | `view-keeps-nothing` |
| `VIEW needs a GRID` | VIEW is a GRID operation; a slice has no strides to keep | SLICE (s, start, len) for a sub-slice | `view-of-a-slice` |
| `cannot write through the RO field s` | the record came in RO, so its fields are read-only too | take it as VAR if it must be written | `write-through-readonly-field` |
| `cannot write through the RO parameter r` | RO is a read-only borrow | VAR | `write-through-readonly-record` |
| `cannot write through the RO parameter s` | the slice came in RO ([READONLY]); its elements cannot be assigned | VAR, or copy into a slice you own | `write-through-readonly-slice` |
| `cannot write through the RO parameter s` | STR is SLICE OF CHAR and an RO one is read-only like any slice | VAR, or build a new string with DynStr | `write-through-readonly-str` |
| `cannot write through a value parameter` | a value PTR parameter is a SHARED borrow: readable, not writable (par 4.1) | declare the parameter VAR p: PTR T -- the mutable-handle idiom | `write-through-value-ptr` |
| `cannot pass SLICE OF CHAR where I64 is expected` | the argument's type is not the parameter's, and nothing converts implicitly | convert explicitly, or read the signature: docs/modules/<M>.md | `wrong-arg-type` |

## The programs

Each is the smallest program that earns its message.  Reading one is
faster than reading the rule.

### adr-outside-unsafe

`ADR exists only inside UNSAFE modules`

```
MODULE m ;
VAR b : SLICE OF BYTE ; x : I64 ;
PROCEDURE F () = BEGIN G (ADR (b)) END F ;
PROCEDURE G (p: I64) = BEGIN x := p END G ;
END m.
```

### alias-nonscalar-conversion

`unknown procedure: Pair`

```
MODULE m ;
TYPE Pair = RECORD a, b : I64 END ;
VAR x : I64 ;
PROCEDURE F () =
VAR p : Pair ;
BEGIN
  (* an alias for a SCALAR converts under its own name; an alias for
     anything else chases to something that is not a builtin and is
     the unknown procedure it looks like *)
  p := Pair (x)
END F ;
END m.
```

### all-outside-a-view

`cannot assign ALL to I64`

```
MODULE m ;
VAR i : I64 ;
BEGIN i := ALL END m.
```

### arity-mismatch

`G expects 1 argument(s), got 2`

```
MODULE m ;
VAR x : I64 ;
PROCEDURE G (a: I64) = BEGIN x := a END G ;
PROCEDURE F () = BEGIN G (1, 2) END F ;
END m.
```

### byte-arithmetic

`BYTE is a raw octet: no arithmetic`

```
MODULE m ;
VAR a, b : BYTE ; c : I64 ;
PROCEDURE F () = BEGIN c := I64 (a + b) END F ;
END m.
```

### case-label-mismatch

`CASE label is a CHAR/string literal but the selector is I64`

```
MODULE m ;
VAR x : I64 ;
PROCEDURE F () = BEGIN CASE x OF | "a" : x := 0 ELSE x := 1 END END F ;
END m.
```

### case-label-twice

`CASE label 200 appears twice`

```
MODULE CaseLabelTwice ;

(* A repeated label is settled at compile time, and until 2026-08-31
   only the C compiler settled it -- reporting `duplicate case value`
   against generated code the author never wrote.

   Single literal labels only: a range needs interval overlap and a
   CONST designator needs the value, and guessing at either would be
   worse than the gap. *)

PROCEDURE Reason (status: I64) : I64 =
BEGIN
  CASE status OF
  | 200 : RETURN 1
  | 404 : RETURN 2
  | 200 : RETURN 3
  ELSE    RETURN 0
  END
END Reason ;

VAR n : I64 ;

BEGIN
  n := Reason (200)
END CaseLabelTwice.
```

### case-record-selected

`CASE RECORD is reached by CASE, not by selection`

```
MODULE m ;
TYPE T = CASE RECORD | A : x: I64 | B END ;
TYPE R = RECORD t : T END ;
VAR r : R ; y : I64 ;
BEGIN y := r.t.x END m.
```

### compare-mismatch

`cannot compare I64 with SLICE OF CHAR`

```
MODULE m ;
VAR x : I64 ; b : BOOL ;
PROCEDURE F () = BEGIN b := x = "hi" END F ;
END m.
```

### concat-char

`cannot concatenate a string with CHAR`

```
(* a character is a scalar: `s + c` would have to decide silently
   whether c is a character or a number.  DynStr.AppendChar, or a
   one-character literal, which IS a string. *)
MODULE m ;
VAR s, t : SLICE OF CHAR ; c : CHAR ;
PROCEDURE F () =
BEGIN
  t := s + c
END F ;
END m.
```

### concat-non-string

`cannot concatenate a string with an integer literal`

```
MODULE m ;
VAR s, t : SLICE OF CHAR ;
PROCEDURE F () =
BEGIN
  t := s + 1
END F ;
END m.
```

### cond-not-bool

`condition must be BOOL, not I64`

```
MODULE m ;
VAR x : I64 ;
PROCEDURE F () = BEGIN IF x THEN x := 0 END END F ;
END m.
```

### conditional-move

`use of s after it was consumed by SHARED`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
VAR g : SHARED PTR R ; b : BOOL ; x : I64 ;
PROCEDURE F () = VAR s : PTR R ;
BEGIN s := NEW (R) ;
IF b THEN g := SHARED (s) END ;
x := s.v END F ;
END m.
```

### def-not-implemented

`declared in the definition but not implemented`

```
DEFINITION MODULE d ;
PROCEDURE F () : I64 ;
END d.
IMPLEMENTATION MODULE d ;
END d.
```

### dispose-a-borrow

`a borrow is not yours to free`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE F (p: PTR R) = BEGIN DISPOSE (p) END F ;
END m.
```

### dispose-pool-interior

`the pool owns p; free the pool`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE F () = VAR pool : POOL ; p : PTR R IN pool ;
BEGIN p := NEW (pool, R) ; DISPOSE (p) END F ;
END m.
```

### div-on-float

`DIV is integer division`

```
MODULE m ;
VAR a, b : F64 ;
PROCEDURE F () = BEGIN a := b DIV b END F ;
END m.
```

### grid-len-axis-exists

`axis 2 of a GRID 2 OF F64 does not exist`

```
MODULE m ;
VAR g : GRID 2 OF F64 ; i : I64 ;
BEGIN i := LEN (g, 2) END m.
```

### grid-len-needs-an-axis

`LEN of a GRID needs an axis`

```
MODULE m ;
VAR g : GRID 2 OF F64 ; i : I64 ;
BEGIN i := LEN (g) END m.
```

### grid-rank-is-part-of-the-type

`cannot assign GRID 3 OF F64 to GRID 2 OF F64`

```
MODULE m ;
VAR a : GRID 2 OF F64 ; b : GRID 3 OF F64 ;
BEGIN a := b END m.
```

### grid-subscript-arity

`a GRID 3 needs 3 subscripts, not 2`

```
MODULE m ;
VAR g : GRID 3 OF F64 ; x : F64 ; i : I64 ;
BEGIN x := g[i, i] END m.
```

### implicit-widening

`cannot assign I32 to I64`

```
MODULE m ;
VAR a : I32 ; b : I64 ;
PROCEDURE F () = BEGIN b := a END F ;
END m.
```

### int-literal-to-float

`cannot assign an integer literal to F64`

```
MODULE m ;
VAR f : F64 ;
PROCEDURE F () = BEGIN f := 1 END F ;
END m.
```

### int-slash-division

`float division; integers use DIV`

```
MODULE m ;
VAR a, b : I64 ;
PROCEDURE F () = BEGIN a := b / b END F ;
END m.
```

### is-some-on-non-opt

`IS SOME needs an OPT operand`

```
MODULE m ;
VAR x : I64 ; y : I64 ;
PROCEDURE F () = BEGIN IF x IS SOME p THEN y := 0 END END F ;
END m.
```

### kept-borrow-arg

`argument 1 of Note: borrowed msg is kept by the callee -- declare KEPT msg (par 4.1)`

```
MODULE m ;
VAR last : STR ;
PROCEDURE Note (RO KEPT msg: STR) =
BEGIN
  last := msg
END Note ;
PROCEDURE Relay (RO msg: STR) =
BEGIN
  Note (msg)
END Relay ;
BEGIN
END m.
```

### kept-concat-arg

`argument 1 of Note: a KEPT parameter cannot take a concatenation -- it dies with this frame (par 4.1)`

```
MODULE m ;
VAR last : STR ;
PROCEDURE Note (RO KEPT msg: STR) =
BEGIN
  last := msg
END Note ;
PROCEDURE Go () =
BEGIN
  Note ('ab' + 'cd')
END Go ;
BEGIN
END m.
```

### kept-undeclared

`undeclared retention: borrowed msg reaches module state -- declare KEPT msg (par 4.1)`

```
MODULE m ;
VAR last : STR ;
PROCEDURE Note (RO msg: STR) =
BEGIN
  last := msg
END Note ;
BEGIN
END m.
```

### kept-via-local

`undeclared retention: borrowed msg (carried by t) reaches module state -- declare KEPT msg (par 4.1)`

```
MODULE m ;
VAR last : STR ;
PROCEDURE Note (RO msg: STR) =
VAR t : STR ;
BEGIN
  t := msg ;
  last := t
END Note ;
BEGIN
END m.
```

### lend-value-ptr-as-var

`cannot lend the value parameter p as VAR`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE G (VAR p: PTR R) = BEGIN p.v := 0 END G ;
PROCEDURE F (p: PTR R) = BEGIN G (p) END F ;
END m.
```

### local-const-shadow

`a local CONST may not shadow a module CONST: Tag`

```
MODULE m ;

CONST Tag = 'module' ;

PROCEDURE P () =
CONST Tag = 'local' ;
BEGIN
  Tag
END P ;

BEGIN
  P ()
END m.
```

### module-state-without-stateful

`module-level state requires STATEFUL on the definition`

```
DEFINITION MODULE m ;
PROCEDURE Bump () : I64 ;
END m.
IMPLEMENTATION MODULE m ;
VAR n : I64 ;
PROCEDURE Bump () : I64 = BEGIN n := n + 1 ; RETURN n END Bump ;
END m.
```

### monitor-outside

`monitor field n is reached from outside a procedure bound to the monitor (par 6)`

```
MODULE MonitorOutside ;

(* par 6: all access to a monitor's fields is serialised, and the way
   that is made true is that only a BOUND procedure may reach them.
   A bound procedure is one whose FIRST parameter is the monitor --
   M9 has no method syntax, so the parameter IS the binding.

   Bump is bound and legal.  The module body is not bound, so reading
   the same field there reaches past the lock. *)

TYPE
  Gate = MONITOR RECORD
    n : I64 ;
  END ;

PROCEDURE Bump (VAR g: Gate) =
BEGIN
  g.n := g.n + 1        (* bound: this is the binding, and it is fine *)
END Bump ;

VAR
  gate : Gate ;
  k : I64 ;

BEGIN
  Bump (gate) ;
  k := gate.n           (* not bound: refused *)
END MonitorOutside.
```

### move-borrow-into-own

`cannot move borrowed q into an OWN parameter`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE G (OWN p: PTR R) = BEGIN DISPOSE (p) END G ;
PROCEDURE F (q: PTR R) = BEGIN G (q) END F ;
END m.
```

### new-reversed

`NEW takes the pool first, then the type`

```
MODULE m ;
TYPE Point = RECORD x : F64 ; END ;
VAR
  pool : POOL ;
  p : PTR Point IN pool ;
BEGIN
  p := NEW (Point, pool) ;
  p.x := 1.0
END m.
```

### no-such-field

`no field w`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
VAR r : R ; x : I64 ;
PROCEDURE F () = BEGIN x := r.w END F ;
END m.
```

### opaque-not-defined

`opaque type not defined in the implementation`

```
DEFINITION MODULE d ;
TYPE T ;
END d.
IMPLEMENTATION MODULE d ;
END d.
```

### opt-through-field

`OPT value used without IS SOME guard`

```
MODULE m ;
TYPE R = RECORD nxt : OPT PTR R ; v : I64 END ;
VAR p : PTR R ; x : I64 ;
BEGIN x := p.nxt.v END m.
```

### pool-escape-on-return

`pool-interior pointer escapes its pool`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE F () : PTR R = 
VAR pool : POOL ; r : PTR R IN pool ;
BEGIN r := NEW (pool, R) ; RETURN r END F ;
END m.
```

### pure-allocates

`cannot allocate from the pool pool in a PURE procedure (par 3.2)`

```
MODULE PureAllocates ;

(* The last effect a PURE body could still have.  NEW is a builtin, so
   the callee rule does not see it, and it is not an assignment
   target, so the write rules do not either -- yet allocating from a
   pool the CALLER owns consumes the caller's storage and answers a
   slice that lives in the caller's arena.

   A pool declared LOCAL is invisible outside the frame and stays
   legal: the test is the pool's binding mode, not its presence. *)

PROCEDURE Alloc (VAR pool: POOL ; n: I64) : SLICE OF I64 [PURE] =
BEGIN
  RETURN NEW (pool, I64, n)
END Alloc ;

VAR
  p : POOL ;
  s : SLICE OF I64 ;

BEGIN
  s := Alloc (p, 4)
END PureAllocates.
```

### pure-calls-impure

`PURE procedure calls Note, which is not PURE (par 3.2)`

```
MODULE PureCallsImpure ;

(* par 3.2 rule 3: a PURE procedure may call only PURE procedures.
   That is what makes "no I/O" true without the checker knowing what
   I/O is -- Io.WriteLine is not PURE, and a foreign procedure carries
   [SERIAL] or [REENTRANT] and never [PURE], so neither is reachable
   from a PURE body.  Purity is transitive by construction.

   Spelled with a local impure procedure rather than an import,
   because a probe is checked on its own. *)

VAR seen : I64 ;

PROCEDURE Note (x: I64) =
BEGIN
  seen := seen + x
END Note ;

PROCEDURE Twice (x: I64) : I64 [PURE] =
BEGIN
  Note (x) ;
  RETURN x * 2
END Twice ;

VAR n : I64 ;

BEGIN
  seen := 0 ;
  n := Twice (21)
END PureCallsImpure.
```

### pure-writes-var

`cannot write through the VAR parameter acc in a PURE procedure (par 3.2)`

```
MODULE PureWritesVar ;

(* par 3.2: a PURE procedure has no observable effect.  Writing
   through a caller's VAR binding is observable by the caller, which
   is the whole point of VAR. *)

PROCEDURE Add (VAR acc: I64 ; x: I64) : I64 [PURE] =
BEGIN
  acc := acc + x ;
  RETURN acc
END Add ;

VAR a : I64 ;

BEGIN
  a := 0 ;
  a := Add (a, 1)
END PureWritesVar.
```

### result-discarded

`function result discarded: G returns I64`

```
MODULE m ;
PROCEDURE G () : I64 = BEGIN RETURN 1 END G ;
PROCEDURE F () = BEGIN G () END F ;
END m.
```

### retention-ledgered

`retention: borrowed x stored into r`

```
MODULE m ;
TYPE R = RECORD s : SLICE OF CHAR END ;
PROCEDURE F (VAR r: PTR R ; x: SLICE OF CHAR) = 
BEGIN r.s := x END F ;
END m.
```

### return-mismatch

`cannot RETURN SLICE OF CHAR from a function of type I64`

```
MODULE m ;
PROCEDURE F () : I64 = BEGIN RETURN "hi" END F ;
END m.
```

### return-value-in-proper-proc

`RETURN with a value in a proper procedure`

```
MODULE m ;
PROCEDURE F () = BEGIN RETURN 1 END F ;
END m.
```

### signature-differs-from-definition

`signature differs from definition`

```
DEFINITION MODULE m ;
PROCEDURE F (a: I64) : I64 ;
END m.
IMPLEMENTATION MODULE m ;
PROCEDURE F (a: F64) : I64 = BEGIN RETURN 0 END F ;
END m.
```

### totality-uses-selector-type

`is not total (missing R)`

```
DEFINITION MODULE a ;
TYPE V = CASE RECORD | P | Q END ;
END a.
DEFINITION MODULE b ;
TYPE V = CASE RECORD | P | Q | R END ;
END b.
MODULE m ;
IMPORT b ;
VAR v : b.V ; x : I64 ;
PROCEDURE F () = BEGIN
  CASE v OF | P : x := 1 | Q : x := 2 END END F ;
END m.
```

### unknown-callee

`unknown procedure: Nonexistent`

```
MODULE m ;
PROCEDURE F () = BEGIN Nonexistent (1) END F ;
END m.
```

### use-after-dispose

`use of s after it was DISPOSEd`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
VAR x : I64 ;
PROCEDURE F () = VAR s : PTR R ;
BEGIN s := NEW (R) ; DISPOSE (s) ; x := s.v END F ;
END m.
```

### use-after-move-assign

`use of s after it was moved`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
VAR x : I64 ;
PROCEDURE F () = VAR s, t : PTR R ;
BEGIN s := NEW (R) ; t := s ; x := s.v ; DISPOSE (t) END F ;
END m.
```

### use-after-shared

`use of s after it was consumed by SHARED`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
VAR g : SHARED PTR R ; x : I64 ;
PROCEDURE F () = VAR s : PTR R ;
BEGIN s := NEW (R) ; g := SHARED (s) ; x := s.v END F ;
END m.
```

### var-arg-not-designator

`must be a variable (VAR/OWN parameter)`

```
MODULE m ;
PROCEDURE G (VAR a: I64) = BEGIN a := 0 END G ;
PROCEDURE F () = BEGIN G (1 + 2) END F ;
END m.
```

### view-axis-count

`needs 3 axis arguments, not 2`

```
MODULE m ;
VAR g : GRID 3 OF F64 ; h : GRID 2 OF F64 ; i : I64 ;
BEGIN h := VIEW (g, ALL, ALL) END m.
```

### view-keeps-nothing

`a VIEW that keeps no axis is an index`

```
MODULE m ;
VAR g : GRID 2 OF F64 ; x : F64 ; i : I64 ;
BEGIN x := VIEW (g, i, i) END m.
```

### view-of-a-slice

`VIEW needs a GRID`

```
MODULE m ;
VAR s : SLICE OF F64 ; g : GRID 1 OF F64 ;
BEGIN g := VIEW (s, ALL) END m.
```

### write-through-readonly-field

`cannot write through the RO field s`

```
MODULE m ;
TYPE R = RECORD RO s : STR ; n : I64 END ;
VAR r : R ;
PROCEDURE F () = BEGIN r.s[0] := 'x' END F ;
END m.
```

### write-through-readonly-record

`cannot write through the RO parameter r`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE F (RO r: R) = BEGIN r.v := 1 END F ;
END m.
```

### write-through-readonly-slice

`cannot write through the RO parameter s`

```
MODULE m ;
PROCEDURE F (RO s: SLICE OF I64) = BEGIN s[0] := 1 END F ;
END m.
```

### write-through-readonly-str

`cannot write through the RO parameter s`

```
MODULE m ;
PROCEDURE F (RO s: STR) = BEGIN s[0] := 'x' END F ;
END m.
```

### write-through-value-ptr

`cannot write through a value parameter`

```
MODULE m ;
TYPE R = RECORD v : I64 END ;
PROCEDURE F (p: PTR R) = BEGIN p.v := 0 END F ;
END m.
```

### wrong-arg-type

`cannot pass SLICE OF CHAR where I64 is expected`

```
MODULE m ;
VAR x : I64 ;
PROCEDURE G (a: I64) = BEGIN x := a END G ;
PROCEDURE F () = BEGIN G ("hi") END F ;
END m.
```
