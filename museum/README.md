# The Museum

Each file is a program that must **fail to compile**, preserving one
failure observed on 2026-08-20. The `(* EXPECT-ERROR ... *)` line names
the diagnostic the compiler must produce. Phase 2's exit criterion is
this directory rejected in full, with the right messages.
